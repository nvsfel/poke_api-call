# poke_api-call
Desktop application built with Python as a recreational exercise. It consumes external APIs (PokeAPI) to fetch and display real-time Pokémon data through a graphical user interface (GUI)
